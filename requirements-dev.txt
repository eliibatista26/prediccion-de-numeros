# Dependencias de desarrollo (tests). Instalar con:
#   pip install -r requirements-dev.txt
# Incluye las de runtime para poder importar todos los módulos en las pruebas.
-r requirements.txt
pytest==8.4.2
